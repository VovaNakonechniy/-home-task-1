import { Fixture } from './Fixture';
declare const testWithFixture: {
    (title: string, fixturePath: string[], testCallback: (fixture: Fixture) => Promise<void>, only?: boolean | undefined): void;
    only(title: string, fixturePath: string[], testCallback: (fixture: Fixture) => Promise<void>): void;
};
export { testWithFixture };
