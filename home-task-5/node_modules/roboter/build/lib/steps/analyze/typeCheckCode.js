import { compileTypeScript } from '../build/compileTypeScript';
const typeCheckCode = async function ({ applicationRoot }) {
    return compileTypeScript({ applicationRoot, noEmit: true });
};
export { typeCheckCode };
