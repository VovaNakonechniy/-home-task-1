import { Result } from 'defekt';
import * as errors from '../../errors';
declare const lintPackageJson: ({ applicationRoot }: {
    applicationRoot: string;
}) => Promise<Result<undefined, errors.PackageJsonMalformed>>;
export { lintPackageJson };
