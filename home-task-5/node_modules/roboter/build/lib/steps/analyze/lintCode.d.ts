import { Result } from 'defekt';
import * as errors from '../../errors';
declare const lintCode: ({ applicationRoot }: {
    applicationRoot: string;
}) => Promise<Result<undefined, errors.CodeMalformed>>;
export { lintCode };
