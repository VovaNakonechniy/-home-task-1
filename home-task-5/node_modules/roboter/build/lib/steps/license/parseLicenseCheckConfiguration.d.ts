import { LicenseCheckConfiguration } from './LicenseCheckConfiguration';
import { Result } from 'defekt';
import * as errors from '../../errors';
declare const parseLicenseCheckConfiguration: (unparsedValue: object) => Result<LicenseCheckConfiguration, errors.LicenseCheckConfigurationMalformed>;
export { parseLicenseCheckConfiguration };
