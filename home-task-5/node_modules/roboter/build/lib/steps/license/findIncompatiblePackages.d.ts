import { LicenseCheckConfiguration } from './LicenseCheckConfiguration';
interface Package {
    name: string;
    version: string;
    license: string;
    directory: string;
}
declare const findIncompatiblePackages: ({ licenseCheckConfiguration, absoluteDirectory }: {
    licenseCheckConfiguration: LicenseCheckConfiguration;
    absoluteDirectory: string;
}) => Promise<Package[]>;
export { findIncompatiblePackages };
