import { Result } from 'defekt';
import * as errors from '../errors';
declare const buildTask: ({ applicationRoot }: {
    applicationRoot: string;
}) => Promise<Result<undefined, errors.BuildFailed>>;
export { buildTask };
