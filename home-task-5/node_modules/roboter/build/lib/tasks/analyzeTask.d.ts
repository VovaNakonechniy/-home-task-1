import { Result } from 'defekt';
import * as errors from '../errors';
declare const analyzeTask: ({ applicationRoot }: {
    applicationRoot: string;
}) => Promise<Result<undefined, errors.AnalysisFailed>>;
export { analyzeTask };
