import { Result } from 'defekt';
import * as errors from '../errors.js';
declare const runCommand: (command: string, options?: {
    cwd?: string;
    env?: NodeJS.ProcessEnv;
    maxBuffer?: number;
    silent?: boolean;
}) => Promise<Result<{
    exitCode: number;
    stdout: string;
    stderr: string;
}, errors.ExecutableFailed>>;
export { runCommand };
