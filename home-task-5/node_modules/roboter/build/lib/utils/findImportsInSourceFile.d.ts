import { Result } from 'defekt';
import * as errors from '../errors';
declare const findImportsInSourceFile: ({ filePath }: {
    filePath: string;
}) => Promise<Result<Set<string>, errors.CouldNotAnalyzeSourceFile>>;
export { findImportsInSourceFile };
