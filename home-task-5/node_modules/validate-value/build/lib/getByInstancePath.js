"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getByInstancePath = void 0;
const getByInstancePath = function ({ object, instancePath }) {
    const pathSegments = instancePath.replace(/\//ug, '.').split('.');
    const nonEmptyPathSegments = pathSegments.filter((segment) => segment !== '');
    let value = object;
    for (const pathSegment of nonEmptyPathSegments) {
        if (value === undefined) {
            break;
        }
        value = value[pathSegment];
    }
    return value;
};
exports.getByInstancePath = getByInstancePath;
