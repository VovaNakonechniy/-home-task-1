import { Result } from 'defekt';
import * as errors from './errors';
declare class Queue<TItem> {
    protected items: TItem[];
    constructor(...initialItems: TItem[]);
    isEmpty(): boolean;
    push(...items: TItem[]): void;
    pop(): Result<TItem, errors.QueueIsEmpty>;
}
export { Queue };
