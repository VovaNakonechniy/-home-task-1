import { EntryType } from './types/EntryType';
import { Options } from './types/Options';
declare const walk: ({ directory, yields, matches, ignores, followsSymlinks, maximumDepth }: Options) => AsyncIterable<string>;
export { walk, EntryType };
